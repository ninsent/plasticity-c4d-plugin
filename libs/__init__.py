"""
Plasticity C4D Bridge - Bundled Libraries

This package contains third-party libraries bundled with the plugin:
- websockets: WebSocket client library (v10.4)

Note: The compiled speedups module (.so/.pyd) is platform-specific.
The websockets library will fall back to pure Python if the speedups
module doesn't match your platform.
"""
